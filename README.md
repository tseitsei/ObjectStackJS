# ObjectStackJS
Tile stacking game. JavaScript &amp; HTML5.

## Browser UI:

Open `index.html` in a modern browser to play with the new HTML5 interface. The game keeps a local highscore in the browser storage.
